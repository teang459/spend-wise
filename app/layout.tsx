import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Spend Wise — Personal Finance Tracker",
  description:
    "Track your income and expenses in LAK. Categorize transactions, see monthly summaries, and visualize your spending at a glance.",
  applicationName: "Spend Wise",
  openGraph: {
    title: "Spend Wise — Personal Finance Tracker",
    description:
      "Track your income and expenses in LAK with monthly summaries and beautiful charts.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
